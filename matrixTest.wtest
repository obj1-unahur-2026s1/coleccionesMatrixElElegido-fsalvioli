import pasajeros.*
import nave.*

describe "group of tests " {

  test "test matrix" {
    assert.equals(pasajeros.estaEquilibrada(), true)
    assert.equals(neo.saltar(), 50)

    assert.equals(pasajeros.Colision(), pasajeros.listaPasajeros.size() == 0)
  }

}